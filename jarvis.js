export default async function handler(req,res){res.setHeader("Access-Control-Allow-Origin","*");res.setHeader("Access-Control-Allow-Headers","Content-Type");res.setHeader("Access-Control-Allow-Methods","POST, OPTIONS");if(req.method==="OPTIONS")return res.status(204).end();if(req.method!=="POST")return res.status(405).json({error:"Metodo non consentito"});const apiKey=process.env.OPENAI_API_KEY;if(!apiKey)return res.status(500).json({error:"OPENAI_API_KEY non configurata sul server"});const {message,history=[]}=req.body||{};if(!message||typeof message!=="string")return res.status(400).json({error:"Messaggio mancante"});const trimmed=Array.isArray(history)?history.slice(-10):[];const input=[...trimmed.map(x=>({role:x.role==="assistant"?"assistant":"user",content:String(x.content||"").slice(0,4000)})),{role:"user",content:message.slice(0,6000)}];try{const r=await fetch("https://api.openai.com/v1/responses",{method:"POST",headers:{Authorization:`Bearer ${apiKey}`,"Content-Type":"application/json"},body:JSON.stringify({model:"gpt-5.6-luna",instructions:"Sei JARVIS, assistente personale di Dave. Rispondi in italiano, in modo brillante, pratico, sintetico e affidabile. Quando mancano dati, dillo chiaramente. Non fingere di poter compiere azioni che non puoi compiere.",input})});const data=await r.json();if(!r.ok)return res.status(r.status).json({error:data?.error?.message||"Errore OpenAI API"});let answer="";for(const item of(data.output||[])){for(const part of(item.content||[])){if(part.type==="output_text"&&part.text)answer+=part.text}}if(!answer)answer="Non ho ricevuto testo dal modello.";return res.status(200).json({answer})}catch(e){return res.status(500).json({error:e.message||"Errore server"})}}